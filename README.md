This is a project to copy a sheet from one Excel to another, bringing over the values and formatting but not formulas, so that lighter copies can be accessed easily for analysis.

One file is a generic program to move one sheet to another. 

The second is specific to Urban Institute DYNASIM output that will also give a custom name to the new sheet so that multiple options' results can be put in the same Excel.
